The source to my little gallery of projects! You can find it [here](https://dagothig.com).

Built using Gatsby.js because I'd figured it'd be a decent little project, although it's clearly
overkill for something this size. This is simply built using `yarn` and deployed using `rsync`;
you just have to pop an http server in front of it.
